export default function ({ store, redirect }) {
  // If the user is not authenticated
  // if (!this.$auth.loggedIn) {
  //   this.$router.push('/auth/login')
  // }
  // let data  = store.state.MODULE_AUTH.login;
  console.log('cccccc',data);
}